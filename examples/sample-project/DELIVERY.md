# Delivery

## Demo — 文案不是成片 (v2, end-to-end verified)

- Format: 1080×1920, 30fps, H.264 + AAC, 10.4s
- Route: swiss-sketch (3 scenes, narration-driven timing)
- Narration: AI TTS (10.0s), word timestamps via offline ASR (`coli asr`)
- Captions: SRT + ASS generated by `build_captions.py`, burned last by `finalize.py`

## Verified outputs

| Artifact | Path | Check |
|---|---|---|
| Visual master (HyperFrames render) | `renders/visual-master.mp4` | lint 0 errors; ffprobe 10.4s 1080×1920 h264 |
| Fallback-render proof | `renders/demo-swiss.mp4` | `render-browser.mjs`, 312 frames, visually verified |
| Final delivery | `renders/final-demo.mp4` | narration mixed, loudnorm, captions burned; ffprobe + frame inspection PASS |
| Contact sheet | `verify/contact-sheet.jpg` | scene midpoints s01/s02/s03 |
| Verification report | `verify/report.json` | verdict PASS |
| Captions | `captions/captions.srt`, `captions.ass` | 4 cues, scene-bounded, flash-cue merged |

## Regression status

- `validate_scenes.py` passes (12-route enum).
- All Python scripts compile; `doctor.py` reports core ready (ffmpeg/ffprobe).
- `render-browser.mjs` passes `node --check` and rendered the demo successfully.
- HyperFrames composition lint: 0 errors, 0 warnings (vendored gsap, local @font-face).

## Known notes

- `tts_edge.py` is an optional adapter; `edge_tts` is not installed in this environment (script exits 3 with guidance).
- The HyperFrames CLI was installed locally (`npm install hyperframes`) after the initial `npx` resolution hang; the bundled fallback renderer exists precisely so rendering never blocks on that.
